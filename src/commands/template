const BaseCommand = require('../utils/structures/BaseCommand');

module.exports = class TemplateCommand extends BaseCommand {
    constructor() {
        super('template', 'template', ['aliases']);
    }

    async run(client, message, args) {
        console.log('template');
    }
}
